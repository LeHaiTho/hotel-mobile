import React from 'react';
import PropTypes from 'prop-types';
import {Text} from 'react-native';

const HotelCard = () => {
  return <Text>HotelCard</Text>;
};

HotelCard.propTypes = {};

export default HotelCard;
